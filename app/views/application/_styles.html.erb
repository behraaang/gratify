<% if I18n.locale == :fa %>
    <link href="<%= asset_url('materialize-rtl.min.css') %>" rel="stylesheet">
<% else %>
<% end %>
<style>
  <% if I18n.locale == :fa %>
  * { font-family: Neirizi;
    line-height: 30px !important;

  }
  @font-face {
    font-family: 'BNazanin';
    font-style: normal;
    font-weight: 400;
    src: <%= asset_url('BYekan.ttf') %>;
    unicode-range: U+30-39;
  }
  .number {
    font-family: BNazanin
  }

  h5 {
    line-height: 40px !important;

  }
  .nav-wrapper {
    padding-top: 20px
  }

  .card .card-content {
    padding: 14px
  }
  .ir {    box-shadow: 0 0 0 3pt rgba(45, 129, 255, 0.4);
  }
  header, main, footer {
    padding-right: 300px;
    padding-left: 0;
  }
  @media only screen and (max-width : 992px) {
    header, main, footer {
      padding-right: 0;
      padding-left: 0;
    }
  }
  <% else %>
  .gb {
    box-shadow: 0 0 0 3pt rgba(45, 129, 255, 0.4);
  }

  <% end %>


</style>
<script>
    <% if I18n.locale == :fa %>

    $(document).ready(function(){

          $('.button-collapse').sideNav({
          menuWidth: 300, // Default is 300
          closeOnClick: true, // Closes side-nav on <a> clicks, useful for Angular/Meteor
          draggable: true, // Choose whether you can drag to open on touch screens,
          edge: 'right',
          onOpen: function (el) {
          }
      });
    });
    <% end %>


</script>